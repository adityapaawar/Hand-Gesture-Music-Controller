opencv-python
mediapipe
pyautogui
numpy

If you want to fix versions to ensure compatibility, you can use something like:

ini
Copy code
opencv-python==4.12.0.88
mediapipe==0.10.14
pyautogui==0.9.55
numpy==1.25.2
To install all dependencies at once:

bash
Copy code
pip install -r requirements.txt
